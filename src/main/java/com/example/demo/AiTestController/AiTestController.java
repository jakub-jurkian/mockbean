package com.example.demo.AiTestController;

// Debug endpoint removed — was used only for smoke-testing the Ollama connection
// during Phase 0. Kept as empty class to avoid breaking the package structure
// until the directory can be cleaned up manually.
class AiTestController {}
